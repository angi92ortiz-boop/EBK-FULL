-- [[ ACUSADO NINJA HUB MASHMELLOW - KING'PARRA MASTER DOMINA V6 MODIFICADO ]] --
-- ULTRA OPTIMIZACIÓN FPS | CONTADOR DE GANANCIAS | TELEPORTS EXCLUSIVOS EN MOTO

local _K = "BACK01"
local lp = game:GetService("Players").LocalPlayer
local vu = game:GetService("VirtualUser")
local coreGui = game:GetService("CoreGui")
local runService = game:GetService("RunService")
local http = game:GetService("HttpService")
local playersService = game:GetService("Players")

-- ==========================================
-- [ NUEVO: SISTEMA DE SEGURIDAD ANTI-ADMIN ]
-- ==========================================
local function EscanearStaff(player)
    if player == lp then return end
    
    if game.CreatorType == Enum.CreatorType.User and player.UserId == game.CreatorId then
        lp:Kick("\n[KING'PARRA SHIELD]\nDueño del juego detectado: " .. player.Name)
    elseif game.CreatorType == Enum.CreatorType.Group then
        pcall(function()
            if player:GetRankInGroup(game.CreatorId) >= 10 then 
                lp:Kick("\n[KING'PARRA SHIELD]\nStaff del grupo oficial detectado: " .. player.Name)
            end
        end)
    end
    
    local nameLower = player.Name:lower()
    local displayLower = player.DisplayName:lower()
    if nameLower:find("admin") or nameLower:find("mod") or nameLower:find("staff") or nameLower:find("owner") or
       displayLower:find("admin") or displayLower:find("mod") or displayLower:find("staff") then
        lp:Kick("\n[KING'PARRA SHIELD]\nPosible Moderador detectado: " .. player.Name)
    end
end

for _, p in pairs(playersService:GetPlayers()) do EscanearStaff(p) end
playersService.PlayerAdded:Connect(EscanearStaff)

-- [ PRECIOS Y VALORES ]
local PRECIO_AGUA = 20
local PRECIO_AZUCAR = 100
local PRECIO_GELATINA = 70

local VENTA_SMALL = 2000
local VENTA_MEDIUM = 2450
local VENTA_LARGE = 4150 

local targetParent = lp:FindFirstChild("PlayerGui") or coreGui
local function CleanUI()
    for _, v in pairs(targetParent:GetChildren()) do
        if v:GetAttribute("DominaTag") then v:Destroy() end
    end
end
CleanUI()

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "Core_" .. http:GenerateGUID(false):sub(1, 8)
ScreenGui.Parent = targetParent
ScreenGui.ResetOnSpawn = false
ScreenGui:SetAttribute("DominaTag", true)

local MainRed = Color3.fromRGB(255, 0, 0)
local DarkBg = Color3.fromRGB(5, 5, 5)
local CardBg = Color3.fromRGB(15, 0, 0)

local fpsActual = 0
runService.RenderStepped:Connect(function(dt)
    fpsActual = math.round(1 / dt)
end)

local function ObtenerDinero()
    local stats = lp:FindFirstChild("leaderstats") or lp:FindFirstChild("Data")
    if stats then
        local cash = stats:FindFirstChild("Cash") or stats:FindFirstChild("Money") or stats:FindFirstChild("Dinero")
        if cash then return cash.Value end
    end
    return 0
end

-- ==========================================
-- [ SOLUCIÓN PUNTO 5: SISTEMA DE CACHÉ FIJO ]
-- ==========================================
local cachedPrompts = {Water = {}, Sugar = {}, Gelatin = {}, Sell = {}}

local function AddToCache(v)
    if v:IsA("ProximityPrompt") then
        local name = (v.ObjectText or ""):lower() .. (v.Parent and v.Parent.Name or ""):lower()
        if name:find("water") or name:find("agua") then 
            table.insert(cachedPrompts.Water, v)
        elseif name:find("sugar") or name:find("azucar") then 
            table.insert(cachedPrompts.Sugar, v)
        elseif name:find("gelatin") or name:find("gelatina") then 
            table.insert(cachedPrompts.Gelatin, v)
        end
    end
    if v.Name:lower():find("sell") or v.Name:lower():find("buyer") or (v:IsA("ProximityPrompt") and (v.ObjectText:lower():find("sell") or v.ActionText:lower():find("sell"))) then
        table.insert(cachedPrompts.Sell, v)
    end
end

local function RecargarCacheCompleto()
    table.clear(cachedPrompts.Water)
    table.clear(cachedPrompts.Sugar)
    table.clear(cachedPrompts.Gelatin)
    table.clear(cachedPrompts.Sell)
    for _, v in pairs(workspace:GetDescendants()) do
        AddToCache(v)
    end
end
RecargarCacheCompleto()
workspace.DescendantAdded:Connect(AddToCache)

-- [ LOGIN SCREEN ]
local LoginFrame = Instance.new("Frame", ScreenGui)
LoginFrame.Size = UDim2.new(0, 300, 0, 200)
LoginFrame.Position = UDim2.new(0.5, -150, 0.4, -100)
LoginFrame.BackgroundColor3 = DarkBg
Instance.new("UICorner", LoginFrame)
Instance.new("UIStroke", LoginFrame).Color = MainRed

local LTitle = Instance.new("TextLabel", LoginFrame)
LTitle.Size = UDim2.new(1, 0, 0, 40)
LTitle.Text = "KING'PARRA DOMINA"
LTitle.TextColor3 = MainRed
LTitle.Font = Enum.Font.GothamBold
LTitle.TextSize = 20
LTitle.BackgroundTransparency = 1

local KeyInput = Instance.new("TextBox", LoginFrame)
KeyInput.Size = UDim2.new(0.8, 0, 0, 40)
KeyInput.Position = UDim2.new(0.1, 0, 0.4, 0)
KeyInput.PlaceholderText = "ENTER KEY"
KeyInput.BackgroundColor3 = CardBg
KeyInput.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyInput.Font = Enum.Font.Gotham
KeyInput.TextSize = 14
Instance.new("UICorner", KeyInput)

local LoginBtn = Instance.new("TextButton", LoginFrame)
LoginBtn.Size = UDim2.new(0.8, 0, 0, 40)
LoginBtn.Position = UDim2.new(0.1, 0, 0.75, 0)
LoginBtn.BackgroundColor3 = MainRed
LoginBtn.Text = "VERIFICAR"
LoginBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
LoginBtn.Font = Enum.Font.GothamBold
LoginBtn.TextSize = 14
Instance.new("UICorner", LoginBtn)

-- [ HUB CONTROL PRINCIPAL ]
local function StartHub()
    LoginFrame:Destroy()
    
    local MainFrame = Instance.new("Frame", ScreenGui)
    MainFrame.Size = UDim2.new(0, 560, 0, 470)
    MainFrame.Position = UDim2.new(0.5, -280, 0.5, -235)
    MainFrame.BackgroundColor3 = DarkBg
    MainFrame.Active = true
    MainFrame.Draggable = true
    Instance.new("UICorner", MainFrame)
    Instance.new("UIStroke", MainFrame).Color = MainRed

    local OpenBtn = Instance.new("TextButton", ScreenGui)
    OpenBtn.Size = UDim2.new(0, 100, 0, 35)
    OpenBtn.Position = UDim2.new(0, 10, 0.4, 0)
    OpenBtn.BackgroundColor3 = DarkBg
    OpenBtn.Text = "ABRIR HUD"
    OpenBtn.TextColor3 = MainRed
    OpenBtn.Visible = false
    OpenBtn.Font = Enum.Font.GothamBold
    OpenBtn.TextSize = 12
    Instance.new("UICorner", OpenBtn)
    Instance.new("UIStroke", OpenBtn).Color = MainRed

    local Title = Instance.new("TextLabel", MainFrame)
    Title.Size = UDim2.new(1, 0, 0, 40)
    Title.Text = "ACUSADO NINJA HUB MASHMELLOW - KING'PARRA"
    Title.TextColor3 = MainRed
    Title.Font = Enum.Font.GothamBold
    Title.TextSize = 15
    Title.BackgroundTransparency = 1

    local CloseBtn = Instance.new("TextButton", MainFrame)
    CloseBtn.Size = UDim2.new(0, 30, 0, 30)
    CloseBtn.Position = UDim2.new(1, -35, 0, 5)
    CloseBtn.BackgroundTransparency = 1
    CloseBtn.Text = "X"
    CloseBtn.TextColor3 = MainRed
    CloseBtn.Font = Enum.Font.GothamBold

    local MiniBtn = Instance.new("TextButton", MainFrame)
    MiniBtn.Size = UDim2.new(0, 30, 0, 30)
    MiniBtn.Position = UDim2.new(1, -65, 0, 5)
    MiniBtn.BackgroundTransparency = 1
    MiniBtn.Text = "-"
    MiniBtn.TextColor3 = MainRed
    MiniBtn.Font = Enum.Font.GothamBold

    local InfoLabel = Instance.new("TextLabel", MainFrame)
    InfoLabel.Size = UDim2.new(0.46, 0, 0, 385)
    InfoLabel.Position = UDim2.new(0.03, 0, 0, 65)
    InfoLabel.BackgroundColor3 = CardBg
    InfoLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    InfoLabel.Font = Enum.Font.Code
    InfoLabel.TextSize = 11
    InfoLabel.TextXAlignment = Enum.TextXAlignment.Left
    InfoLabel.TextYAlignment = Enum.TextYAlignment.Top
    Instance.new("UICorner", InfoLabel)

    local function CreateBtn(txt, pos)
        local b = Instance.new("TextButton", MainFrame)
        b.Size = UDim2.new(0.23, 0, 0, 42)
        b.Position = pos
        b.BackgroundColor3 = Color3.fromRGB(30, 0, 0)
        b.TextColor3 = Color3.fromRGB(255, 255, 255)
        b.Font = Enum.Font.GothamBold
        b.Text = txt
        b.TextSize = 9
        Instance.new("UICorner", b)
        return b
    end

    local function CreateInput(placeholder, pos)
        local box = Instance.new("TextBox", MainFrame)
        box.Size = UDim2.new(0.14, 0, 0, 35)
        box.Position = pos
        box.BackgroundColor3 = CardBg
        box.PlaceholderText = placeholder
        box.Text = "0"
        box.TextColor3 = Color3.fromRGB(255, 255, 255)
        box.Font = Enum.Font.Gotham
        box.TextSize = 12
        Instance.new("UICorner", box)
        return box
    end

    local FarmBtn = CreateBtn("FARM MASHMELLOW", UDim2.new(0.51, 0, 0, 65))
    local SellBtn = CreateBtn("SELL MASHMELLOW", UDim2.new(0.75, 0, 0, 65))
    local ToolBtn = CreateBtn("MASHMELLOW TOOL", UDim2.new(0.51, 0, 0, 115))
    local InstantBtn = CreateBtn("INSTANT INTERACT", UDim2.new(0.75, 0, 0, 115))
    local AntiKillBtn = CreateBtn("ANTI-KILL (OFF)", UDim2.new(0.51, 0, 0, 165))
    local AutoBuyBtn = CreateBtn("AUTO-BUY (OFF)", UDim2.new(0.75, 0, 0, 165))
    local FpsBtn = CreateBtn("OPTIMIZAR FPS", UDim2.new(0.51, 0, 0, 215))
    FpsBtn.Size = UDim2.new(0.47, 0, 0, 42)

    local BuyTitle = Instance.new("TextLabel", MainFrame)
    BuyTitle.Size = UDim2.new(0.46, 0, 0, 20)
    BuyTitle.Position = UDim2.new(0.51, 0, 0, 270)
    BuyTitle.Text = "REGULAR CANTIDAD AUTO-COMPRA:"
    BuyTitle.TextColor3 = MainRed
    BuyTitle.Font = Enum.Font.GothamBold
    BuyTitle.TextSize = 11
    BuyTitle.BackgroundTransparency = 1
    BuyTitle.TextXAlignment = Enum.TextXAlignment.Left

    local WaterBox = CreateInput("Agua", UDim2.new(0.51, 0, 0, 295))
    local SugarBox = CreateInput("Azúcar", UDim2.new(0.67, 0, 0, 295))
    local GelatinBox = CreateInput("Gelat.", UDim2.new(0.83, 0, 0, 295))

    local TpTitle = Instance.new("TextLabel", MainFrame)
    TpTitle.Size = UDim2.new(0.46, 0, 0, 20)
    TpTitle.Position = UDim2.new(0.51, 0, 0, 345)
    TpTitle.Text = "TELEPORTS EXCLUSIVOS (SOLO EN MOTO):"
    TpTitle.TextColor3 = MainRed
    TpTitle.Font = Enum.Font.GothamBold
    TpTitle.TextSize = 10
    TpTitle.BackgroundTransparency = 1
    TpTitle.TextXAlignment = Enum.TextXAlignment.Left

    local function CreateTpBtn(txt, pos)
        local b = Instance.new("TextButton", MainFrame)
        b.Size = UDim2.new(0.14, 0, 0, 38)
        b.Position = pos
        b.BackgroundColor3 = Color3.fromRGB(45, 0, 0)
        b.TextColor3 = Color3.fromRGB(255, 255, 255)
        b.Font = Enum.Font.GothamBold
        b.Text = txt
        b.TextSize = 8
        b.TextWrapped = true
        Instance.new("UICorner", b)
        Instance.new("UIStroke", b).Color = Color3.fromRGB(100, 0, 0)
        return b
    end

    local Tp1Btn = CreateTpBtn("Compra/Vender 🍬", UDim2.new(0.51, 0, 0, 370))
    local Tp2Btn = CreateTpBtn("Depa 1️⃣", UDim2.new(0.67, 0, 0, 370))
    local Tp3Btn = CreateTpBtn("Depa 2️⃣", UDim2.new(0.83, 0, 0, 370))

    local farmActive, sellActive, instantActive, antiKillActive, autoBuyActive, fpsOptimized = false, false, false, false, false, false
    local items = {"Water", "Sugar Block Bag", "Gelatin", "Empty Bag"}
    local safeCoords = Vector3.new(519.27, 3.59, 606.96)
    local lastHealth = 100
    local costoTotalCalculado = 0
    local dineroMalvaviscosTotales = 0

    CloseBtn.MouseButton1Click:Connect(function() ScreenGui:Destroy() end)
    MiniBtn.MouseButton1Click:Connect(function() MainFrame.Visible = false OpenBtn.Visible = true end)
    OpenBtn.MouseButton1Click:Connect(function() MainFrame.Visible = true OpenBtn.Visible = false end)

    local function EjecutarTeleportMoto(boton, vectorDestino, textoOriginal)
        local char = lp.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if hum and hum.Health > 0 then
            local seat = hum.SeatPart
            if seat and (seat:IsA("VehicleSeat") or seat:IsA("Seat")) then
                local vehiculo = seat.Parent
                if vehiculo:IsA("Model") then
                    if vehiculo.PrimaryPart then vehiculo:SetPrimaryPartCFrame(CFrame.new(vectorDestino)) else vehiculo:MoveTo(vectorDestino) end
                else
                    seat.CFrame = CFrame.new(vectorDestino)
                end
            else
                boton.Text = "¡SUBE A UNA MOTO!"
                boton.TextColor3 = MainRed
                task.wait(1.2)
                boton.Text = textoOriginal
                boton.TextColor3 = Color3.fromRGB(255, 255, 255)
            end
        end
    end

    Tp1Btn.MouseButton1Click:Connect(function() EjecutarTeleportMoto(Tp1Btn, Vector3.new(519.27, 3.59, 606.96), "Compra/Vender 🍬") end)
    Tp2Btn.MouseButton1Click:Connect(function() EjecutarTeleportMoto(Tp2Btn, Vector3.new(925.26, 10.11, 40.25), "Depa 1️⃣") end)
    Tp3Btn.MouseButton1Click:Connect(function() EjecutarTeleportMoto(Tp3Btn, Vector3.new(896.53, 10.11, 41.55), "Depa 2️⃣") end)

    FpsBtn.MouseButton1Click:Connect(function()
        fpsOptimized = not fpsOptimized
        FpsBtn.BackgroundColor3 = fpsOptimized and Color3.fromRGB(150, 0, 0) or Color3.fromRGB(30, 0, 0)
        if fpsOptimized then
            pcall(function()
                game:GetService("Lighting").GlobalShadows = false
                game:GetService("Lighting").FogEnd = 9e9
                for _, effect in pairs(game:GetService("Lighting"):GetChildren()) do
                    if effect:IsA("PostEffect") or effect:IsA("LightingEffect") then effect.Enabled = false end
                end
                for _, v in pairs(workspace:GetDescendants()) do
                    if v:IsA("BasePart") then
                        v.Material = Enum.Material.SmoothPlastic
                        v.Reflectance = 0
                        v.CastShadow = false
                        if v:IsA("MeshPart") then v.TextureID = "" end
                    elseif v:IsA("Decal") or v:IsA("Texture") or v:IsA("Sky") or v:IsA("Atmosphere") or v:IsA("Clouds") then
                        v:Destroy()
                    elseif v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Sparkles") or v:IsA("Fire") or v:IsA("Smoke") then
                        v.Enabled = false
                    end
                end
            end)
        end
    end)

    -- Función auxiliar para contar inventario de forma rápida y limpia
    local function RecuentarInventario()
        local c = {W=0, S=0, G=0, B=0, Sm=0, M=0, L=0}
        local char = lp.Character
        if not char then return c end
        local inv = lp.Backpack:GetChildren()
        for _, v in pairs(char:GetChildren()) do table.insert(inv, v) end
        for _, i in pairs(inv) do
            if i.Name == "Water" then c.W = c.W + 1 
            elseif i.Name == "Sugar Block Bag" then c.S = c.S + 1 
            elseif i.Name == "Gelatin" then c.G = c.G + 1 
            elseif i.Name == "Empty Bag" then c.B = c.B + 1 
            elseif i.Name:find("Small") then c.Sm = c.Sm + 1 
            elseif i.Name:find("Medium") then c.M = c.M + 1 
            elseif i.Name:find("Large") then c.L = c.L + 1 end
        end
        return c
    end

    FarmBtn.MouseButton1Click:Connect(function()
        farmActive = not farmActive
        FarmBtn.Text = farmActive and "STOP FARM" or "FARM MASHMELLOW"
        FarmBtn.BackgroundColor3 = farmActive and Color3.fromRGB(150, 0, 0) or Color3.fromRGB(30, 0, 0)
        
        task.spawn(function()
            while farmActive do
                local char = lp.Character
                local hum = char and char:FindFirstChildOfClass("Humanoid")
                if hum and hum.Health > 0 then
                    for _, itemName in ipairs(items) do
                        if not farmActive then break end
                        local tool = lp.Backpack:FindFirstChild(itemName) or char:FindFirstChild(itemName)
                        if tool then
                            if tool.Parent == lp.Backpack then 
                                hum:EquipTool(tool)
                                repeat task.wait() until tool.Parent == char or not farmActive
                            end
                            if farmActive and tool.Parent == char then
                                tool:Activate()
                                task.wait(0.12)
                            end
                        end
                    end
                end
                task.wait(0.05)
            end
        end)
    end)

    SellBtn.MouseButton1Click:Connect(function()
        sellActive = not sellActive
        SellBtn.Text = sellActive and "AUTO-SELL ON" or "SELL MASHMELLOW"
        SellBtn.BackgroundColor3 = sellActive and Color3.fromRGB(150, 0, 0) or Color3.fromRGB(30, 0, 0)
    end)

    ToolBtn.MouseButton1Click:Connect(function()
        local t = Instance.new("Tool", lp.Backpack)
        t.Name = "MASHMELLOW DELETE"
        t.RequiresHandle = false
        t.Activated:Connect(function()
            local m = lp:GetMouse()
            if m.Target then pcall(function() m.Target:Destroy() end) end
        end)
    end)

    local function SetupPrompt(p)
        if p:IsA("ProximityPrompt") then p.HoldDuration = 0.01 end
    end

    InstantBtn.MouseButton1Click:Connect(function()
        instantActive = not instantActive
        InstantBtn.Text = instantActive and "INSTANT ON" or "INSTANT INTERACT"
        InstantBtn.BackgroundColor3 = instantActive and Color3.fromRGB(150, 0, 0) or Color3.fromRGB(30, 0, 0)
        if instantActive then
            for _, v in pairs(workspace:GetDescendants()) do SetupPrompt(v) end
        end
    end)

    AntiKillBtn.MouseButton1Click:Connect(function()
        antiKillActive = not antiKillActive
        AntiKillBtn.Text = antiKillActive and "ANTI-KILL (ON)" or "ANTI-KILL (OFF)"
        AntiKillBtn.BackgroundColor3 = antiKillActive and Color3.fromRGB(150, 0, 0) or Color3.fromRGB(30, 0, 0)
        if lp.Character and lp.Character:FindFirstChild("Humanoid") then
            lastHealth = lp.Character.Humanoid.Health
        end
    end)

    -- ==========================================
    -- [ MEJORA: ANTI-KILL OPTIMIZADO A 0.2 SEG ]
    -- ==========================================
    task.spawn(function()
        while task.wait(0.2) do -- Cambiado exactamente a 0.2 segundos
            if antiKillActive then
                local char = lp.Character
                local hum = char and char:FindFirstChildOfClass("Humanoid")
                if hum and hum.Health > 0 then
                    if hum.Health < lastHealth then
                        lastHealth = hum.Health -- Se actualiza inmediatamente para evitar bucles corruptos
                        
                        local seat = hum.SeatPart
                        if seat and (seat:IsA("VehicleSeat") or seat:IsA("Seat")) then
                            local model = seat.Parent
                            if model:IsA("Model") then
                                if model.PrimaryPart then model:SetPrimaryPartCFrame(CFrame.new(safeCoords)) else model:MoveTo(safeCoords) end
                            else
                                seat.CFrame = CFrame.new(safeCoords)
                            end
                        else
                            local hrp = char:FindFirstChild("HumanoidRootPart")
                            if hrp then hrp.CFrame = CFrame.new(safeCoords) end
                        end
                        task.wait(1) -- Cooldown de 1 segundo de inmunidad al TP para evitar trabas por veneno/fuego continuo
                    end
                    lastHealth = hum.Health
                end
            end
        end
    end)

    AutoBuyBtn.MouseButton1Click:Connect(function()
        autoBuyActive = not autoBuyActive
        AutoBuyBtn.Text = autoBuyActive and "AUTO-BUY (ON)" or "AUTO-BUY (OFF)"
        AutoBuyBtn.BackgroundColor3 = autoBuyActive and Color3.fromRGB(150, 0, 0) or Color3.fromRGB(30, 0, 0)
    end)

    -- ==========================================
    -- [ MEJORA: AUTO-BUY SECUENCIAL CON TIEMPOS ]
    -- ==========================================
    task.spawn(function()
        while true do
            if autoBuyActive then
                local dineroActual = ObtenerDinero()
                local c = RecuentarInventario()
                
                local targetW = tonumber(WaterBox.Text) or 0
                local targetS = tonumber(SugarBox.Text) or 0
                local targetG = tonumber(GelatinBox.Text) or 0

                -- 1. Intenta comprar AGUA
                if c.W < targetW and dineroActual >= PRECIO_AGUA then
                    local p = cachedPrompts.Water[1]
                    if p and p.Parent then
                        fireproximityprompt(p)
                        task.wait(2) -- Espera 2 segundos exactos tras comprar Agua
                    end
                end

                -- 2. Intenta comprar AZÚCAR
                if autoBuyActive and c.S < targetS and dineroActual >= PRECIO_AZUCAR then
                    local p = cachedPrompts.Sugar[1]
                    if p and p.Parent then
                        fireproximityprompt(p)
                        task.wait(2) -- Espera 2 segundos exactos tras comprar Azúcar
                    end
                end

                -- 3. Intenta comprar GELATINA
                if autoBuyActive and c.G < targetG and dineroActual >= PRECIO_GELATINA then
                    local p = cachedPrompts.Gelatin[1]
                    if p and p.Parent then
                        fireproximityprompt(p)
                        task.wait(2) -- Espera 2 segundos exactos tras comprar Gelatina
                    end
                end
            end
            task.wait(0.3) -- Descanso general del bucle
        end
    end)

    -- BUCLE PRINCIPAL (Solo para actualizar textos y auto-vender mediante Caché)
    task.spawn(function()
        while task.wait(0.4) do
            FpsBtn.Text = string.format("OPTIMIZAR FPS | FPS: %d", fpsActual)

            local char = lp.Character
            if not char or not char:FindFirstChild("HumanoidRootPart") then continue end

            local c = RecuentarInventario()

            local targetW = tonumber(WaterBox.Text) or 0
            local targetS = tonumber(SugarBox.Text) or 0
            local targetG = tonumber(GelatinBox.Text) or 0

            local reqW = math.max(0, targetW - c.W)
            local reqS = math.max(0, targetS - c.S)
            local reqG = math.max(0, targetG - c.G)

            costoTotalCalculado = (reqW * PRECIO_AGUA) + (reqS * PRECIO_AZUCAR) + (reqG * PRECIO_GELATINA)
            dineroMalvaviscosTotales = (c.Sm * VENTA_SMALL) + (c.M * VENTA_MEDIUM) + (c.L * VENTA_LARGE)
            local dineroActual = ObtenerDinero()

            -- Auto-Sell optimizado con la Caché del Punto 5
            if sellActive then
                local isNear = false
                for _, v in pairs(cachedPrompts.Sell) do
                    if v.Parent then
                        local pos = v:IsA("Model") and v.PrimaryPart and v.PrimaryPart.Position or (v:IsA("BasePart") and v.Position) or (v:IsA("ProximityPrompt") and v.Parent:IsA("BasePart") and v.Parent.Position)
                        if pos and (char.HumanoidRootPart.Position - pos).Magnitude < 15 then
                            isNear = true
                            break
                        end
                    end
                end
                if isNear then
                    pcall(function()
                        for _, i in pairs(lp.Backpack:GetChildren()) do
                            if i.Name:find("Small") or i.Name:find("Medium") or i.Name:find("Large") then
                                char.Humanoid:EquipTool(i)
                                task.wait(0.05)
                                i:Activate()
                            end
                        end
                    end)
                end
            end

            InfoLabel.Text = string.format([[
  [ KING'PARRA MASHMELLOW HUB ]
  PERFORMANCE: EXTREME STABLE
  
  FINANZAS DEL JUGADOR:
  - Dinero Actual: $%d
  - Gasto Compra: $%d
  - Dinero Acumulado: $%d

  PRODUCTOS EN MANO:
  - Small:    %d  |  
  - Medium:   %d  |  
  - Large:    %d  |

  MATERIALES CONTADOS:
  - Agua ($20):     %d / %d (Faltan: %d)
  - Azucar ($100):  %d / %d (Faltan: %d)
  - Gelatina ($70): %d / %d (Faltan: %d)
  - Bolsas:  %d

  [ SEGURIDAD BYPASS ]
  Bypass Core: Activo (Anti-Kick)
  Anti-Kill Status: %s
  SB Texturas: %s
            ]], 
            dineroActual, costoTotalCalculado, dineroMalvaviscosTotales,
            c.Sm, c.M, c.L, 
            c.W, targetW, reqW,
            c.S, targetS, reqS,
            c.G, targetG, reqG,
            c.B,
            antiKillActive and "ON" or "OFF",
            fpsOptimized and "ULTRA BOOST" or "NORMAL")
        end
    end)
end

LoginBtn.MouseButton1Click:Connect(function()
    if KeyInput.Text == _K then StartHub() else LoginBtn.Text = "ERROR" task.wait(1) LoginBtn.Text = "VERIFICAR" end
end)

lp.Idled:Connect(function() 
    vu:CaptureController() 
    vu:ClickButton2(Vector2.new()) 
end)
